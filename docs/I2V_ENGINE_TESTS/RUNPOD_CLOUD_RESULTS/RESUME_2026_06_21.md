# REPRISE — Session du 2026-06-21 (atelier vidéo cloud Luna)

Objectif final : application/moteur MAISON qui dépasse Kling sur n'importe quelle scène
d'action et n'importe quel personnage, sans payer d'API externe.

## Où on en est (tout est sauvegardé)

Tout est sur GitHub `byakuyakutchiki/yawatch-luna-stories` (branche master, HEAD `b78ca16`).
Rien n'est perdu en arrêtant le pod : les modèles (32 Go) restent sur le volume `/workspace`.

### Acquis solides
- Pipeline FramePack cloud opérationnel sur RunPod (RTX PRO 4500), génération ~6-8 min/clip.
- Quality Gate I2V automatique fonctionne (SSIM identité, lumière, flicker).
- NOUVELLE métrique `app/motion_metrics.py` : sépare "mouvement organique localisé" de
  "cadre photo qui glisse" (translation globale). C'est l'outil clé pour piloter la qualité.

### 4 itérations mesurées (PLAN02 Luna) — voir framepack_motion_iterations_v1_v4/
| ver | réglage | organic | risk_glisse | SSIM | verdict |
|---|---|---:|---:|---:|---|
| v1 | teacache ON, g10 | 0.279 | 0.09 | 0.955 | figé |
| v2 | teacache OFF, g7, seed neuf | 0.612 | 0.34 | 0.439 | bouge mais glisse + identité cassée |
| v3 | teacache OFF, g9, seed v1, anti-mvt | 0.189 | 0.03 | 0.969 | trop figé |
| v4 | teacache OFF, g8, seed v1 | 0.266 | 0.045 | 0.928 | meilleur compromis stable, mais retenu |

Les 4 MP4 sont dans `C:\Users\saint\Downloads\` pour visionnage humain de Ludovic.

### Conclusion data-driven (le mur)
FramePack-HY échange mouvement contre identité : impossible d'avoir "vivant + identité + sans
glissement" sur une image unique par tuning de paramètres. Décision = CHOIX DE MOTEUR, pas réglage.

## Décision en attente (à trancher demain, avec Ludo + DeepSeek/ChatGPT)
1. Accepter FramePack v4 = look "premium retenu".
2. Tester un I2V à fort mouvement : **Wan2.2 I2V**, **LTX-Video**, **CogVideoX** (risque identité).
3. **Pipeline 2 étages** : modèle-mouvement + restauration/lock du visage (le plus proche d'un
   "Kling maison", + d'ingénierie) — piste la plus prometteuse pour l'objectif final.
4. (faible espoir) FramePack-F1 ou latent_window_size plus court.

## Comment reprendre demain (checklist pod)
1. RunPod → démarrer le pod → noter le NOUVEAU port "Direct TCP ports" (il change à chaque start).
2. Re-autoriser la clé SSH (le /root est effacé au restart) — UNE ligne dans le terminal web :
   `mkdir -p ~/.ssh && printf '%s\n' '<clé publique runpod_luna>' >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys`
3. Réinstaller les deps (effacées au restart, /workspace persiste) :
   `cd /workspace/ComfyUI && /usr/local/bin/python -m pip install --break-system-packages -r requirements.txt blake3 tqdm sqlalchemy alembic && pip install --break-system-packages -r custom_nodes/ComfyUI-FramePackWrapper/requirements.txt`
4. Relancer : `setsid /usr/local/bin/python main.py --listen 127.0.0.1 --port 8188 &`
5. Les modèles FramePack (16+16 Go) sont déjà sur le volume — rien à re-télécharger.

GOUVERNANCE : passer par MotionDirector + bibliothèques pour préparer les jobs ; ne plus bricoler
le JSON à la main. FramePack n'est pas encore intégré au MotionDirector (à faire si on garde FramePack).
