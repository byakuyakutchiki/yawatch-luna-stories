pyyaml>=6.0
python-dotenv>=1.0.0
openai>=1.0.0
pytest>=7.0.0
Pillow>=10.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
celery>=5.3.0
redis>=5.0.0
# Optionnel — décommenter pour l'assemblage vidéo MoviePy
# moviepy>=2.0.0
#
# Dépendances système (installer via apt) :
#   sudo apt install ffmpeg imagemagick -y
